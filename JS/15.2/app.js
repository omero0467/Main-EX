function findSmallest(a, b, c){
    if(a > b > c){
        return c;
    } else if(a > c > b) 
    {return a;}
    else {
        return b;
    }}
    findSmallest(52,66, 2);

    //1 line num 9
    //2 i used debbuger in vs code 
    //3 the invokation used the wrong name to a non exsiting function.
    //4 code fixed on line 9